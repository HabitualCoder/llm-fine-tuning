# Core training stack
transformers>=4.44
datasets>=2.20
trl>=0.9.6
peft>=0.13
accelerate>=0.34
evaluate>=0.4
huggingface_hub>=0.24
safetensors>=0.4

# PyTorch: install the right build for your system/GPU first if needed.
# For CPU-only you can use plain 'torch'. For NVIDIA CUDA 12.1, for example:
#   pip install --index-url https://download.pytorch.org/whl/cu121 torch torchvision torchaudio
torch

# Optional (advanced): 4-bit quantized fine-tuning
# bitsandbytes>=0.43
